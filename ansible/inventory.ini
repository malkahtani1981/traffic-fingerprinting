# Ansible inventory for the traffic-fingerprinting toolchain.
# Replace the host address with the IP output by Terraform (see ../terraform).
#
#   ansible-playbook -i inventory.ini playbook.yml

[fingerprint_nodes]
# vm1 ansible_host=203.0.113.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa

[fingerprint_nodes:vars]
ansible_python_interpreter=/usr/bin/python3
